micronaut:
  application:
    name: pseudo-service
  config-client:
    enabled: true
gcp:
  project-id: dev-sirius
  credentials:
    location: private/gcp/sa-keys/dev-dapla-pseudo-service-test-sa-key.json

  secret-manager:
    keys:
      - testsecret
